import { BengaliDatePipePipe } from './bengali-date-pipe.pipe';

describe('BengaliDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new BengaliDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
